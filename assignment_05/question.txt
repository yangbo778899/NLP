1.overfitting & underfitting 的原因
 underfitting欠拟合：
     1.模型训练不够，模型过于简单，不能充分表达现实世界的特征。
     2.特征量过少。
     3.过度正则化。
     4.数据太复杂


 overfitting过拟合：
     1.训练集太少
     2.训练集抽样不均匀，有偏差
     3.训练过度，模型太过复杂
     4.训练数据噪音过大（数据问题）
     5.正则化太少
